# Fork Notes

Source: https://github.com/gothinkster/node-express-realworld-example-app
Cloned (depth 1) and `.git` stripped so this directory is tracked as part of
the parent `nexus-brownfield-handoff-demo` repo's own history.

## Status
Not yet read for conventions. Before either agent touches this codebase,
read the auth scheme and migration tooling here and write findings to
`../docs/decisions/` - same brownfield discipline the Pulse repo used.
